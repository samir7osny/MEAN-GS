{
  "name": "mean-gs",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "pushd %cd%\\front-end && ng build && popd && pushd %cd%\\back-end && node app && popd",
    "build": "ng build",
    "test": "ng test",
    "lint": "ng lint",
    "e2e": "ng e2e"
  }
}
